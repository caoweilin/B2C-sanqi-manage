<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sanqi.manage.mapper.ContentMapper">
	<select id="queryContentList">
		select * from tb_content where id = #{categoryId} order by updated desc
	</select>
</mapper>
